<%- include("partials/header") -%>
    <h1>Home</h1>
    <p>
        <%= homeStartingContent %>
    </p>
    <% posts.forEach(function(post){ %>
        <h1><%= post.titleInput %></h1>
        <p class="blogInput">
            <% <%= post.blogInput %>.slice(0, 100) %>
        </p>
    <% }) %>
<%- include("partials/footer") -%>